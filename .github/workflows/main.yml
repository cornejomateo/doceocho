name: Send emails

on:
  schedule:
    - cron: "0 10 * * *"
  workflow_dispatch:

jobs:
  call-function:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase Edge Function
        run: |
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            https://fbkvdpljgxdyxlfqcyol.functions.supabase.co/send-birthday-emails
